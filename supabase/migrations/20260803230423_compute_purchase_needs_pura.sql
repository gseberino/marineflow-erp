-- Versão registrada em produção em 03/08/2026 23:04 com o nome 'compute_purchase_needs_pura' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260803123000_compute_purchase_needs_pura.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
