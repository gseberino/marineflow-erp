-- Versão registrada em produção em 06/07/2026 23:52 com o nome 'custom_payment_installments' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260706110000_custom_payment_installments.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
