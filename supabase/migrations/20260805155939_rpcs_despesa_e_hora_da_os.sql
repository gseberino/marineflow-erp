-- Versão registrada em produção em 05/08/2026 15:59 com o nome 'rpcs_despesa_e_hora_da_os' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260805150000_rpcs_despesa_e_hora_da_os.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
