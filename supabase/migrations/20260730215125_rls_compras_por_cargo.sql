-- Versão registrada em produção em 30/07/2026 21:51 com o nome 'rls_compras_por_cargo' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260730120000_rls_compras_por_cargo.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
