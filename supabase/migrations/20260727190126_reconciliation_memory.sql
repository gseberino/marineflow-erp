-- Versão registrada em produção em 27/07/2026 19:01 com o nome 'reconciliation_memory' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260727180000_reconciliation_memory.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
