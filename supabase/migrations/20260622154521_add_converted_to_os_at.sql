-- Versão registrada em produção em 22/06/2026 15:45 com o nome 'add_converted_to_os_at' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260622000000_add_converted_to_os_at.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
