-- Versão registrada em produção em 23/06/2026 22:03 com o nome 'saved_filters_default' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260623000000_saved_filters_default.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
