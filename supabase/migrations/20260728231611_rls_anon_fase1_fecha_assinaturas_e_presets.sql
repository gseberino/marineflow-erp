-- Versão registrada em produção em 28/07/2026 23:16 com o nome 'rls_anon_fase1_fecha_assinaturas_e_presets' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260728015000_rls_anon_fase1_fecha_assinaturas_e_presets.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
