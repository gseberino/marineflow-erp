-- Versão registrada em produção em 25/07/2026 01:33 com o nome 'stock_v2_status_sets_fix' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260724220000_stock_v2_status_sets_fix.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
