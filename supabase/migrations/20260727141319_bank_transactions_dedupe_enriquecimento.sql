-- Versão registrada em produção em 27/07/2026 14:13 com o nome 'bank_transactions_dedupe_enriquecimento' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260727120000_bank_transactions_dedupe_enriquecimento.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
