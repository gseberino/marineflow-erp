-- Versão registrada em produção em 09/09/2026 18:59 com o nome 'agenda_suggestions_quote_request_entity' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260828110000_agenda_suggestions_quote_request_entity.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
