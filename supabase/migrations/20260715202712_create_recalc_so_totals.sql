-- Versão registrada em produção em 15/07/2026 20:27 com o nome 'create_recalc_so_totals' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260715163000_create_recalc_so_totals.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
