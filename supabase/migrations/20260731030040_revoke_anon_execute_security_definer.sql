-- Versão registrada em produção em 31/07/2026 03:00 com o nome 'revoke_anon_execute_security_definer' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260729120000_revoke_anon_execute_security_definer.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
