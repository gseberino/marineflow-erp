-- Versão registrada em produção em 26/07/2026 18:34 com o nome 'agenda_inbox_detector_cron' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260726161000_agenda_inbox_detector_cron.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
