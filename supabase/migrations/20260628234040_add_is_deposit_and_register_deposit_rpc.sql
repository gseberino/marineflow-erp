-- Versão registrada em produção em 28/06/2026 23:40 com o nome 'add_is_deposit_and_register_deposit_rpc' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260629000001_add_is_deposit_and_register_deposit_rpc.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
