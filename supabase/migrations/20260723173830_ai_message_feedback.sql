-- Versão registrada em produção em 23/07/2026 17:38 com o nome 'ai_message_feedback' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260723150000_ai_message_feedback.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
