-- Versão registrada em produção em 17/07/2026 14:10 com o nome 'fiscal_pdf_storage_path' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260717140000_fiscal_pdf_storage_path.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
