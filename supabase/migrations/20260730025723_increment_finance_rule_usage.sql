-- Versão registrada em produção em 30/07/2026 02:57 com o nome 'increment_finance_rule_usage' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260729231000_increment_finance_rule_usage.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
