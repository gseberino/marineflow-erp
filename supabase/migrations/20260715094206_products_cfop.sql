-- Versão registrada em produção em 15/07/2026 09:42 com o nome 'products_cfop' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260715090000_products_cfop.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
