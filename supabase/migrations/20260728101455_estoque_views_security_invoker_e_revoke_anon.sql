-- Versão registrada em produção em 28/07/2026 10:14 com o nome 'estoque_views_security_invoker_e_revoke_anon' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260728012500_estoque_views_security_invoker_e_revoke_anon.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
