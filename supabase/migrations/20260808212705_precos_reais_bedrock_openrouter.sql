-- Versão registrada em produção em 08/08/2026 21:27 com o nome 'precos_reais_bedrock_openrouter' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260808160000_precos_reais_bedrock_openrouter.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
