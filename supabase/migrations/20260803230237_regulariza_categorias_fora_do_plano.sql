-- Versão registrada em produção em 03/08/2026 23:02 com o nome 'regulariza_categorias_fora_do_plano' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260803010000_regulariza_categorias_fora_do_plano.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
