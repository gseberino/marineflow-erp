-- Versão registrada em produção em 29/06/2026 00:47 com o nome 'commission_sync_trigger' e SEM arquivo no repositório
-- (MF-AUD-058). O SQL original não está em nenhum worktree; o estado que ela produziu está
-- capturado em supabase/schemas/producao/. Este arquivo existe para o histórico local casar com
-- o remoto e o `db push` voltar a funcionar. Não faz nada de propósito.
select 1 where false;
