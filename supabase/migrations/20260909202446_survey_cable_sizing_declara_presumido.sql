-- Versão registrada em produção em 09/09/2026 20:24 com o nome 'survey_cable_sizing_declara_presumido' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260909170000_survey_cable_sizing_declara_presumido.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
