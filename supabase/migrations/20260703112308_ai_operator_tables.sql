-- Versão registrada em produção em 03/07/2026 11:23 com o nome 'ai_operator_tables' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260703140000_ai_operator_tables.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
