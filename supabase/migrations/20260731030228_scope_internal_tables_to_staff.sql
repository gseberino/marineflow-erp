-- Versão registrada em produção em 31/07/2026 03:02 com o nome 'scope_internal_tables_to_staff' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260729130000_scope_internal_tables_to_staff.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
