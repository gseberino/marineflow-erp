-- Versão registrada em produção em 24/07/2026 16:49 com o nome 'product_fiscal_complete_trigger' (MF-AUD-058).
-- DUPLICATA: o mesmo conteúdo está em 20260724160000_product_fiscal_complete_trigger.sql, que se auto-registra sob outra versão —
-- foi aplicada primeiro pela ferramenta (MCP apply_migration), que atribui o próprio timestamp.
-- Este arquivo existe só para o histórico local casar com o remoto. Não faz nada de propósito.
select 1 where false;
